services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: feedsense-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - PORT=8080
      - NODE_ENV=production
    restart: always
